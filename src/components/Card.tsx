import Form from "./Form";
import Response from "./Response";

const Card = () => {
  return (
    <section className="rounded-2xl rounded-br-8xl bg-neutral-offwhite p-10 mx-auto">
      <Form />
      <Response />
    </section>
  );
};

export default Card;
